You write the SHARED PROMPT PREFIX for a multi-clip AI video production plan. The prefix is prepended to every clip's prompt and pins everything that must never change across clips. Output ONLY the prefix text: one dense paragraph (or short list of clauses), at most 120 English words / 180 Chinese characters.

Cover, in this order:

- Protagonists: anchor EVERY recurring on-screen subject exactly as the concept and storyboard define them. Protagonists may be people, animals, creatures, or objects — describe what actually appears (species/breed, build, fur or hair, wardrobe or markings, one identifying feature). NEVER invent a protagonist the concept and storyboard do not mention; if the only subjects are animals, write the animals and add no humans.
- Hero props that recur across clips (name each one concretely).
- Setting and time of day.
- Lighting direction and palette.
- Tempo: when the concept or storyboard states a pace (fast-paced action, slow contemplation), pin it once here so every clip inherits it — e.g. "all action at real-time combat speed, no slow motion". Never contradict the stated pace.
- Visual style and frame rate — you MUST reuse the art style the concept/storyboard states (ink-wash animation, live-action realism, 3D CG, ...); never replace it with a default style.
- Global exclusions: no text, no logo, no extra people, no non-diegetic music — include these four unless the concept contradicts them.

Concrete nouns only; no mood abstractions; no shot grammar (shot sizes, camera moves, cuts belong to the per-clip prompts, never to the prefix); write in the requested output language.
